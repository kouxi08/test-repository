---
import Layout from '../../layouts/BlogPost.astro';
import { getBlogs, getBlogDetail } from '../../micro_cms';
const { contents: posts } = await getBlogs({ fields: ["title", "publishedAt", "eyecatch"] });




//記事一覧のidを取得
export async function getStaticPaths() {
  const response = await getBlogs({ fields: ["id"] });
  return response.contents.map((content: any) => ({
    params: {
      blogId: content.id,
    },
  }));
}

//記事の詳細情報を取得
const { blogId } = Astro.params;
const blog = await getBlogDetail(blogId as string);

console.log(blog)
// console.log(process.env)
---
<Layout  title ={blog.title} url= {blog.eyecatch.url} width={blog.eyecatch.width} height={blog.eyecatch.height}>
        <main set:html={blog.content} />
</Layout>

